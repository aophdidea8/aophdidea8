(function($) {
	/*
		Get the submit button,
		Stop it submitting
		Poke the lat/long to postcode and check if the return is the same as the postcode submitted
		If use the values from the hidden fields if we have them and the postcode is the same.
		Else different get the lat/long from the postcode.
		Call the lastfm API page with lat/long to get the events
		Populate the events article with the event details based on a pre-sorted format.
	*/
})(jQuery);